[package]
name = "ocel_cli"
version = "0.1.0"
edition = "2021"
description = "Headless batch interface for the FLOWVAULT OCEL core."
license = "MIT"

[dependencies]
ocel_core = { path = "../ocel_core" }
serde_json = "1"
